{
  "question" : {
    "question_id" : 79581846,
    "title" : "How to automate move-classes-refactor into new Maven/Gradle modules?",
    "body" : "<p><code>Aside:</code> <em>Longest-winded question I've ever made ... appreciate your patience.</em></p>\n<h3>Background</h3>\n<p>An organization has built a few micro-services using <code>Spring Boot</code>. Each of these live in distinct <code>Git</code> repos but sharing common a build pipeline and artifact repository. Each of these implementations are presently construed as a single <code>Maven</code> or <code>Gradle</code> module. Each of these repos publishes an <code>OpenAPI</code> or <code>Swagger</code> interface document for consumption across the organization – which is lovely because folks using different implementation languages can consume these services.</p>\n<h3>The Aim: Seeking Deeper Communion Among Consumers Employing Implementation Language of Providers</h3>\n<p>It turns out a fair number of consumers of these micro-services are using the same implementation language and dependency stacks as the micro-service implementations. As such, it would be swell if each of these micro-services could publish a collection of classes that bridge the gap between OpenApi data types and semantics not otherwise available from OpenApi code generation.</p>\n<h1>Finally, the Question</h1>\n<p>Is there anything in any IDE or GitLab plugin, etc., which can do this re-org refactor in Java code base (Maven example):</p>\n<h2>From</h2>\n<pre><code>- pom.xml\n- src\n</code></pre>\n<h2>To</h2>\n<p>Wherein <code>public</code> and <code>implementation</code> are arbitrary parameters to the tool:</p>\n<pre><code>- pom.xml\n- pubic/pom.xml\n- public/src\n- implementation/pom.xml\n- implementation/src\n</code></pre>\n<p>The <em>most important point of this question is:</em></p>\n<p>The tool does the necessary &quot;<em>move-refactor</em>&quot; from:</p>\n<pre><code>- src/**.java\n</code></pre>\n<p>to new <code>implementation</code> module :</p>\n<pre><code>- implementation/src/**.java\n</code></pre>\n<p>. . . and leaves moving</p>\n<pre><code>- implementation/src\n</code></pre>\n<p>into</p>\n<pre><code>- public/src\n</code></pre>\n<p>the responsibility of the tool user.</p>\n<p>I appreciate everyone reading this far.</p>\n",
    "tags" : [ "java", "spring-boot", "maven", "gradle", "intellij-idea" ],
    "owner" : {
      "account_id" : 22466,
      "reputation" : 11279,
      "user_id" : 55452,
      "user_type" : "registered",
      "accept_rate" : 100,
      "profile_image" : "https://i.sstatic.net/n7uAEtPN.jpg?s=256",
      "display_name" : "Dave L.",
      "link" : "https://stackoverflow.com/users/55452/dave-l"
    },
    "is_answered" : false,
    "view_count" : 55,
    "answer_count" : 0,
    "score" : 0,
    "last_activity_date" : 1745017092,
    "creation_date" : 1745017092,
    "link" : "https://stackoverflow.com/questions/79581846/how-to-automate-move-classes-refactor-into-new-maven-gradle-modules",
    "content_license" : "CC BY-SA 4.0"
  },
  "answers" : [ ],
  "question_comments" : [ ],
  "answer_comments" : { }
}