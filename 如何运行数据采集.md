# 如何运行数据采集工具

## ⚡ 最简单的方法（推荐）

### Windows 用户

直接运行提供的批处理脚本：

```cmd
collect-data.bat 1000 Sample_SO_data
```

如果需要使用访问令牌：

```cmd
collect-data.bat 1000 Sample_SO_data your_access_token
```

### Linux/Mac 用户

首先给脚本执行权限（只需一次）：

```bash
chmod +x collect-data.sh
```

然后运行：

```bash
./collect-data.sh 1000 Sample_SO_data
```

如果需要使用访问令牌：

```bash
./collect-data.sh 1000 Sample_SO_data your_access_token
```

## 🔧 如果脚本无法运行

### 方法 1：使用 Maven 直接运行

```bash
# Windows
mvnw.cmd exec:java -Dexec.mainClass="cs209a.finalproject_demo.collector.SimpleDataCollector" -Dexec.args="1000 Sample_SO_data"

# Linux/Mac
./mvnw exec:java -Dexec.mainClass="cs209a.finalproject_demo.collector.SimpleDataCollector" -Dexec.args="1000 Sample_SO_data"
```

### 方法 2：先编译再运行

```bash
# 步骤 1：编译项目
mvnw.cmd clean compile
# 或
./mvnw clean compile

# 步骤 2：使用 Maven 运行
mvnw.cmd exec:java -Dexec.mainClass="cs209a.finalproject_demo.collector.SimpleDataCollector" -Dexec.args="1000 Sample_SO_data"
# 或
./mvnw exec:java -Dexec.mainClass="cs209a.finalproject_demo.collector.SimpleDataCollector" -Dexec.args="1000 Sample_SO_data"
```

## 📝 参数说明

- **第一个参数**：要采集的线程数量（例如：1000）
- **第二个参数**：输出目录（例如：Sample_SO_data）
- **第三个参数**：Stack Overflow 访问令牌（可选，用于提升配额）

## ⚠️ 注意事项

1. **首次运行**：会自动下载 Maven 依赖，可能需要几分钟
2. **网络连接**：需要能够访问 Stack Exchange API
3. **采集时间**：采集 1000 个线程大约需要 20-30 分钟
4. **API 配额**：未认证用户每分钟最多 300 个请求

## 🆘 遇到问题？

1. **找不到 Maven**：确保项目中有 `mvnw.cmd`（Windows）或 `mvnw`（Linux/Mac）文件
2. **编译错误**：检查 JDK 版本（需要 JDK 17 或更高）
3. **网络错误**：检查网络连接，确保能访问 `api.stackexchange.com`

---

更多详细信息请参考：`docs/DataCollection.md`








